# .readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# Use MkDocs for the build
mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false

# Specify Python requirements for the build environment
python:
  install:
    - requirements: docs/requirements.txt
